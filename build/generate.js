// Entry point for HelipadUSA's static-page generator, run automatically by Netlify
// before every deploy (see netlify.toml). Phase 1: proves the build pipeline itself
// works before any real page generation is added in later phases.
console.log("HelipadUSA build: generator running (Node " + process.version + ")");
console.log("HelipadUSA build: no pages to generate yet (Phase 1 foundation).");
